#include <bits/stdc++.h>
using namespace std;
typedef long long LL;

void solve() {
    cout << "b p m f d t n l g k h j q x zh ch sh r z c s y w" << endl;
    cout << "a o e i u v ai ei ui ao ou iu ie ve er an en in un vn ang eng ing ong" << endl;
}

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(0);
    int ts = 1;
    // cin >> ts;
    for (int t = 1; t <= ts; t++) {
        solve();
    }
    return 0;
}